name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *"  # 5분마다 실행

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render API
        run: curl -s https://fullstack-beginner-project.onrender.com/api/companies || true
